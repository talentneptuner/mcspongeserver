# 由Sponge专组汉化
# 汉化：星光灬羽神
# 未经允许严禁搬运！
afk {
    # 是否启用此指令
    enabled=true
}
afkrefresh {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
back {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=5
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
ban {
    # 是否启用此指令
    enabled=true
}
blacklist {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"blacklist.list" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"blacklist.set" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
blockinfo {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
blockzap {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
broadcast {
    # 是否启用此指令
    enabled=true
}
checkban {
    # 是否启用此指令
    enabled=true
}
checkjail {
    # 是否启用此指令
    enabled=true
}
checkmute {
    # 是否启用此指令
    enabled=true
}
checknotes {
    # 是否启用此指令
    enabled=true
}
checkwarnings {
    # 是否启用此指令
    enabled=true
}
clear {
    # 是否启用此指令
    enabled=true
}
clearnotes {
    # 是否启用此指令
    enabled=true
}
clearwarnings {
    # 是否启用此指令
    enabled=true
}
commandspy {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
deletehome {
    # 是否启用此指令
    enabled=true
}
deletehomeother {
    # 是否启用此指令
    enabled=true
}
delnick {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
enchant {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
enderchest {
    # 是否启用此指令
    enabled=true
}
entityinfo {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
exp {
    # 是否启用此指令
    enabled=true
}
feed {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
firstjoinkit {
    # 是否启用此指令
    enabled=true
}
firstspawn {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
fly {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=1
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
freezeplayer {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
gamemode {
    # Time, in seconds, that a player has to wait between uses of this command. Set to 0 to disable.
    cooldown=0
    # Cost of a command that a player has to pay to use the command. The cost will only be deducted if successful. Set to 0 to disable.
    cost=0
    # 是否启用此指令
    enabled=true
    # Time, in seconds, that a player has to wait without moving or using other commands before their command will execute. Set to 0 to disable.
    warmup=0
}
getpos {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
globalmute {
    # 是否启用此指令
    enabled=true
}
god {
    # 是否启用此指令
    enabled=true
}
hat {
    # 是否启用此指令
    enabled=true
}
heal {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
helpop {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=10
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
home {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=5
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
homeother {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=5
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
homeset {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
ignite {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
ignore {
    # 是否启用此指令
    enabled=true
}
info {
    # 是否启用此指令
    enabled=true
}
invsee {
    # 是否启用此指令
    enabled=true
}
itembuy {
    # 是否启用此指令
    enabled=true
}
iteminfo {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
itemname {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"itemname.clear" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"itemname.set" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
itemsell {
    # 是否启用此指令
    enabled=true
}
jail {
    # 是否启用此指令
    enabled=true
}
jails {
    # 是否启用此指令
    enabled=true
}
jump {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
kick {
    # 是否启用此指令
    enabled=true
}
kickall {
    # 是否启用此指令
    enabled=true
}
kill {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
kit {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"kit.give" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"kit.list" {
    aliases {
        kits=true
    }
}
kittycannon {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
lightning {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
list {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
listhomes {
    # 是否启用此指令
    enabled=true
}
lockweather {
    # 是否启用此指令
    enabled=true
}
lore {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"lore.add" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"lore.clear" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"lore.set" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
mail {
    # 是否启用此指令
    enabled=true
}
"mail.send" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
me {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
message {
    aliases {
        tell=true
    }
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
more {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
motd {
    # 是否启用此指令
    enabled=true
}
mute {
    # 是否启用此指令
    enabled=true
}
nameban {
    # 是否启用此指令
    enabled=true
}
nameunban {
    # 是否启用此指令
    enabled=true
}
nick {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
note {
    # 是否启用此指令
    enabled=true
}
nucleus {
    # 是否启用此指令
    enabled=true
}
"nucleus.itemalias" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"nucleus.printperms" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
plainbroadcast {
    # 是否启用此指令
    enabled=true
}
powertool {
    # 是否启用此指令
    enabled=true
}
realname {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
removenote {
    # 是否启用此指令
    enabled=true
}
removewarning {
    # 是否启用此指令
    enabled=true
}
repair {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
rtp {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=5
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
rules {
    # 是否启用此指令
    enabled=true
}
seen {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
serverstat {
    # 是否启用此指令
    enabled=true
}
setfirstspawn {
    # 是否启用此指令
    enabled=true
}
setspawn {
    # 是否启用此指令
    enabled=true
}
setworth {
    # 是否启用此指令
    enabled=true
}
skull {
    # Time, in seconds, that a player has to wait between uses of this command. Set to 0 to disable.
    cooldown=0
    # Cost of a command that a player has to pay to use the command. The cost will only be deducted if successful. Set to 0 to disable.
    cost=0
    # 是否启用此指令
    enabled=true
    # Time, in seconds, that a player has to wait without moving or using other commands before their command will execute. Set to 0 to disable.
    warmup=0
}
socialspy {
    # 是否启用此指令
    enabled=true
}
spawn {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=5
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
spawnmob {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
speed {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
staffchat {
    # 是否启用此指令
    enabled=true
}
stop {
    # 是否启用此指令
    enabled=true
}
sudo {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
suicide {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
teleport {
    aliases {
        tp=true
    }
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
teleportnative {
    # 是否启用此指令
    enabled=true
}
tempban {
    # 是否启用此指令
    enabled=true
}
thru {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
time {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"time.set" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
top {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
tpa {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=3
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
tpaall {
    # 是否启用此指令
    enabled=true
}
tpaccept {
    # 是否启用此指令
    enabled=true
}
tpahere {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
tpall {
    # 是否启用此指令
    enabled=true
}
tpdeny {
    # 是否启用此指令
    enabled=true
}
tphere {
    # 是否启用此指令
    enabled=true
}
tppos {
    # 是否启用此指令
    enabled=true
}
tptoggle {
    # 是否启用此指令
    enabled=true
}
unban {
    # 是否启用此指令
    enabled=true
}
vanish {
    # 是否启用此指令
    enabled=true
}
voice {
    # 是否启用此指令
    enabled=true
}
warn {
    # 是否启用此指令
    enabled=true
}
warp {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"warp.delete" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"warp.list" {
    aliases {
        warps=true
    }
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"warp.set" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
weather {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
world {
    aliases {
        nucleusworld=true
        nworld=true
    }
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"world.border" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"world.border.gen" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"world.create" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"world.list" {
    aliases {
        worlds=true
    }
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"world.setspawn" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"world.spawn" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
"world.teleport" {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
worth {
    # 指令冷却时间，单位为秒，玩家要等待到设置的时间后才可以使用指令，设置0则为禁用此功能。
    cooldown=0
    # 指令花费金钱，玩家需要花费设置的金钱才可以使用指令，如果不够金钱则无法使用此指令，设置为0则禁用此功能。
    cost=0
    # 是否启用此指令
    enabled=true
    # 指令等待时间，单位为秒，玩家需要等待到设置的时间后才可以触发指令【移动和使用指令会打断等待时间】，设置为0则禁用此功能。
    warmup=0
}
